{
  "name": "rustcane-mcp",
  "description": "Rust MCP server for Arcane Docker management — manage containers, images, networks, volumes, and compose projects.",
  "author": "Jacob Magar",
  "repository": "https://github.com/jmagar/rustcane",
  "license": "MIT",
  "keywords": ["rustcane", "arcane", "docker", "containers", "homelab", "mcp"],
  "contextFileName": "GEMINI.md",
  "mcpServers": {
    "rustcane": {
      "type": "http",
      "url": "${settings.server_url}/mcp",
      "headers": {
        "Authorization": "Bearer ${settings.api_token}"
      }
    }
  },
  "skills": "./skills",
  "hooks": "./hooks/hooks.json",
  "settings": [
    {
      "name": "server_url",
      "description": "Base URL of the MCP HTTP server. Keep http://localhost:40060 for local use; set to https://rustcane.yourdomain.com for a remote server. Do NOT include a trailing /mcp.",
      "required": true,
      "default": "http://localhost:40060"
    },
    {
      "name": "api_token",
      "description": "Bearer token for MCP HTTP authentication. Every request to /mcp includes: Authorization: Bearer <token>. Generate one with: openssl rand -hex 32",
      "required": false,
      "sensitive": true
    },
    {
      "name": "rustcane_api_url",
      "description": "Base URL of the upstream Arcane API this server manages Docker through, e.g. https://arcane.example.com. Maps to the RUSTCANE_API_URL env var.",
      "required": false
    },
    {
      "name": "rustcane_api_key",
      "description": "API key or bearer token for authenticating to the upstream Arcane API. Maps to the RUSTCANE_API_KEY env var.",
      "required": false,
      "sensitive": true
    }
  ]
}
